{
  "name": "battleplan",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/battleplan",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/battleplan",
        "outputPath": "dist/apps/battleplan"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/battleplan"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "battleplan:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "battleplan:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "battleplan:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "battleplan:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/battleplan/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/battleplan/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
