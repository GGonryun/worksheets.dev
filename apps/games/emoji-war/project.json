{
  "name": "games-emoji-war",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/games/emoji-war",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/games/emoji-war",
        "outputPath": "dist/apps/games/emoji-war"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/games/emoji-war"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "games-emoji-war:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "games-emoji-war:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "games-emoji-war:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "games-emoji-war:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/games/emoji-war/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/games/emoji-war/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
