assign:
  - list: [1, 2, 3, 4, 5]

steps:
  - for: list
    steps:
      - wait: 1000
      - log: 'index: ${index}, value: ${value}'
      - call: http.request
        input:
          url: https://api.sampleapis.com/beers/ale
          method: GET
        output: resp

  - return: ${resp.body}
