import { servicesAdvertisement } from './services-advertisement';

describe('servicesAdvertisement', () => {
  it('should work', () => {
    expect(servicesAdvertisement()).toEqual('services-advertisement');
  });
});
