# services-twitch

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build services-twitch` to build the library.

## Running unit tests

Run `nx test services-twitch` to execute the unit tests via [Jest](https://jestjs.io).
