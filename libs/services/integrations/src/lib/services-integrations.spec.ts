import { servicesIntegrations } from './services-integrations';

describe('servicesIntegrations', () => {
  it('should work', () => {
    expect(servicesIntegrations()).toEqual('services-integrations');
  });
});
