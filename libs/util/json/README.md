# util-json

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build util-json` to build the library.

## Running unit tests

Run `nx test util-json` to execute the unit tests via [Jest](https://jestjs.io).
