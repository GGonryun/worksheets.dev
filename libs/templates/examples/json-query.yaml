name: query output json
steps:
  - call: http
    input:
      url: https://api.sampleapis.com/beers/ale
      method: GET
    output: resp
  - call: json.query
    input:
      data: ${resp.body}
      selector: '.name'
    output: translate
  - return: ${translate}
