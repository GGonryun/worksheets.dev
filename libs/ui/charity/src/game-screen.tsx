// TODO: play game page, mobile and desktop views.

import { Box } from '@mui/material';

// when resizing the window jump in chunks. do not support fluid resizing.

// show a play game section.

// then the game category breadcrumbs, title, developer, and voting and score.

// then more game suggestions.

// then the game description.

// if a game does not support mobile, show a warning message "This game does not support mobile devices. Please play on a desktop computer."

// on desktop views, show the game in the center and show extra game suggestions on the right and left. one column, two columns and three columns. depending on the screen size.

export const GameScreen = () => {
  return <Box>Game Screen!</Box>;
};
